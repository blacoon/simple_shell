#include <stdio.h>

/**
 * printHelloUser - print hello {username}.
 * @username: the username argument
 * Return: 0 if no error
 */

int printHelloUser(username)
{
	printf("Hello " + username);
	return 0;
}
